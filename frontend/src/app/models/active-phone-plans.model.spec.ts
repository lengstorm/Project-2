import { ActivePhonePlans } from './active-phone-plans.model';

describe('ActivePhonePlans', () => {
  it('should create an instance', () => {
    expect(new ActivePhonePlans()).toBeTruthy();
  });
});
