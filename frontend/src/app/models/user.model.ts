export class User {

    id: number;
    name: string;
    phoneNumber: string;

    constructor(id: number, name: string, phoneNumber: string) {

        this.id = id;
        this.name = name;
        this.phoneNumber = phoneNumber;
    }

}
